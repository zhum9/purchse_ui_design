# Procurement Execution Center - Agent Instructions

本文件是本项目所有 AI/Codex 开发任务的长期约束。

在修改任何代码之前，必须先阅读本文件。

---

## 1. 项目定位

本项目是企业级：

**采购执行中心 Procurement Execution Center**

它是面向采购、收货、仓储、服务验收及 SAP 集成人员使用的业务系统。

它不是：

- SAP MM 的复制版
- 财务系统
- 会计核算系统
- 简单的收货入库系统

核心目标是：

以统一业务体验完成采购订单执行、采购履约、收货、服务验收、限额执行、退货、冲销和 SAP 集成。

---

## 2. 开发前必须阅读的文档

根据任务类型读取对应文档。

### 产品和业务设计

- `docs/*

### 前端架构

- `docs/FRONTEND_ARCHITECTURE.md`

### UI / UX Skills

- `skills/enterprise-ui/SKILL.md`
- `skills/procurement-ui/SKILL.md`
- `skills/dynamic-form/SKILL.md`
- `skills/enterprise-table/SKILL.md`
- `skills/sap-integration-ui/SKILL.md`
- `skills/visual-quality/SKILL.md`

任何新增页面在编码前，必须先判断其属于：

- 列表
- 详情
- 采购履约
- 动态表单
- SAP集成
- 配置页面

并读取对应 Skill。

禁止绕过已有设计规范自行创造新的页面结构和视觉体系。

---

## 3. 核心业务原则

以下原则属于系统长期基础约束。

### 3.1 合同不是采购执行必经节点

系统必须支持：

- 合同 → 采购订单
- 采购申请 → 采购订单
- 寻源结果 → 采购订单
- 直接采购订单
- 外部系统 / SAP采购订单

不得把采购合同设计成所有采购订单的强制前置节点。

### 3.2 采购订单是采购执行主线

核心业务关系：

采购订单  
→ 采购订单行  
→ 执行场景识别  
→ 采购履约  
→ 收货 / 服务验收 / 金额确认  
→ 退货 / 冲销  
→ SAP

### 3.3 采购订单行是最小执行单元

所有履约逻辑必须至少支持：

`PO + PO Item`

同一采购订单允许存在不同执行场景。

### 3.4 物料号不是全局必填

系统必须支持：

- 库存物料采购
- 消耗性采购
- 无物料号采购
- 服务采购
- 限额服务
- 其他扩展采购场景

不得将 `materialCode` 设计为系统级强制必填字段。

### 3.5 服务采购不等于普通收货

服务采购主要执行动作应为：

**服务验收**

而不是普通库存入库。

### 3.6 退货和冲销严格区分

采购退货：

原业务正确，后续真实发生退回供应商。

收货冲销：

原收货业务本身错误，需要撤销原业务影响。

不得通过修改、删除原收货记录实现冲销。

正式业务历史必须保留。

---

## 4. SAP边界

业务系统负责业务事实。

SAP负责ERP凭证、库存会计及财务核算。

业务前台不得要求普通业务人员填写：

- GL Account
- 借方科目
- 贷方科目
- GR/IR
- 库存科目
- 费用科目
- 成本要素

如果SAP采购订单已经存在：

- Cost Center
- WBS
- Asset
- Internal Order
- Account Assignment

原则上：

只读继承、后台保存或由 SAP Adapter 使用。

不要要求采购履约人员重复填写。

SAP技术字段应放在：

**SAP信息 / 技术信息 / 接口日志**

区域，不应成为主要业务页面语言。

---

## 5. 前端技术原则

优先技术栈：

- React
- TypeScript
- Ant Design

复杂表格可以使用：

- TanStack Table

开发要求：

- TypeScript优先
- 避免 `any`
- 公共逻辑抽 Hook
- 公共展示抽 Component
- API统一封装
- 业务类型集中定义
- 枚举集中管理
- 状态颜色集中管理
- 动态表单规则集中管理

禁止：

- 大量复制页面
- 超大单文件组件
- 大量 inline style
- 大量 `!important`
- 魔法数字和魔法字符串
- 将业务规则散落在 JSX 中

---

## 6. UI设计原则

系统采用成熟企业级 ERP / SaaS 后台风格。

关键词：

- 专业
- 克制
- 清晰
- 高信息密度
- 高操作效率
- 业务导向

不要设计成营销网站或数据大屏。

禁止：

- 大面积渐变
- 玻璃拟态
- 复杂3D
- 无意义的大卡片
- 大量装饰插画
- 过度圆角
- 过度阴影

---

## 7. 页面结构规范

### 列表页面

应优先采用：

页面标题  
→ 简短说明  
→ 状态快速筛选  
→ 查询条件  
→ 数据表格  
→ 分页

### 详情页面

应优先采用：

业务摘要  
→ 核心状态  
→ 关键指标  
→ Tabs  
→ 明细内容

### 简单业务操作

优先使用：

右侧大 Drawer

例如：

- 普通收货
- 简单退货
- 收货冲销

### 复杂业务操作

优先使用：

独立页面

例如：

- 复杂服务验收
- 多明细验收
- 多附件业务

---

## 8. 采购履约设计原则

采购履约工作台是系统核心页面。

不要为每种采购场景建立完全独立的主菜单。

统一通过：

**采购履约工作台**

处理不同订单行。

不同执行场景的动作可以不同：

- 库存物料 → 收货
- 无物料号采购 → 收货
- 服务采购 → 服务验收
- 限额服务 → 执行确认

---

## 9. 动态表单原则

不同采购场景不得简单复制多套表单。

应优先使用统一动态字段机制。

字段状态至少支持：

- HIDDEN
- DISPLAY
- OPTIONAL
- REQUIRED
- AUTO

规则至少支持：

- 显示规则
- 必填规则
- 只读规则
- 默认值规则
- 校验规则
- 计算规则

示例：

`batchManaged = true`

→ `batch = REQUIRED`

`executionScenario = SERVICE`

→ 隐藏库存地点、批次  
→ 显示服务期间、验收金额

`materialCode = null`

→ 不显示物料号选择器

---

## 10. 状态规范

禁止使用无法理解的裸数字状态。

例如禁止：

`status = 1`

`status = 13`

必须使用语义明确的枚举。

至少区分：

- documentStatus
- fulfillmentStatus
- receiptStatus
- approvalStatus
- sapSyncStatus

统一状态色：

- 绿色：完成 / 成功
- 蓝色：进行中
- 橙色：待处理 / 预警
- 红色：异常 / 失败
- 灰色：关闭 / 作废

---

## 11. SAP接口状态

至少支持：

- WAITING
- PROCESSING
- SUCCESS
- FAILED
- UNKNOWN

对于 `UNKNOWN`：

禁止直接自动重试SAP过账。

必须先进行SAP状态核对，避免重复凭证。

---

## 12. 危险业务操作

以下属于高风险操作：

- 冲销
- 采购退货
- 强制关闭
- SAP重新执行
- 作废

必须：

- 明确业务影响
- 使用确认机制
- 需要时二次确认
- 清晰记录原因
- 保留原始业务记录

不要将所有操作按钮都设计成红色。

只有真正危险操作使用危险视觉样式。

---

## 13. 页面完整状态

主要页面开发时必须同时考虑：

- Loading
- Empty
- Error
- No Permission
- Success
- Disabled
- Partial
- SAP Failed

禁止只实现理想正常状态。

---

## 14. Mock数据

Mock数据必须具有真实采购业务语义。

建议基础数据：

库存物料：

玉米，订单1000吨，已收700吨。

无物料号：

生产维修耗材，20批，已收12批。

服务采购：

SAP年度运维服务，120万元，已验收60万元。

限额服务：

设备零星维修，最高限额15万元，累计执行6.35万元。

还必须覆盖：

- 部分执行
- 已完成
- 超期
- SAP失败
- 收货冲销
- 采购退货

禁止使用：

- Test1
- AAA
- 商品1
- 用户1

等没有业务语义的Mock数据。

---

## 15. 修改代码前的工作方式

接受任务后：

1. 先检查相关现有代码。
2. 阅读相关 Skill 和设计文档。
3. 判断是否已有可复用组件。
4. 判断修改会影响哪些业务场景。
5. 在现有架构基础上实现。
6. 不随意推翻已有稳定结构。

如果需求存在歧义：

优先采用：

通用、配置化、低耦合的实现。

不要将某个单一客户的特殊要求硬编码成系统全局规则。

---

## 16. 完成任务后的自检

每次完成前端任务后至少检查：

- 是否符合 enterprise-ui
- 是否符合 procurement-ui
- 是否符合 dynamic-form
- 是否符合 enterprise-table
- 是否符合 sap-integration-ui
- 是否符合 visual-quality

同时检查：

- TypeScript错误
- lint
- build
- 页面溢出
- 表格横向滚动
- 空状态
- 加载状态
- 异常状态
- 长文本
- 状态颜色
- 危险操作
- 按钮层级
- 表单必填及校验

如果项目已有自动化测试，应运行相关测试。

不要以“页面能打开”作为完成标准。

---

## 17. 总体开发原则

开发目标不是：

“快速生成一个页面”。

而是：

**持续构建一套统一、可复用、可维护、可以长期演进的企业级采购执行产品。**

任何新增实现都必须优先考虑：

- 是否可复用
- 是否可配置
- 是否符合现有业务模型
- 是否破坏统一视觉
- 是否把SAP复杂度错误暴露给业务人员



## Model Usage Strategy

本项目以“节省额度 + 保证关键任务质量”为原则。

### 默认模型

日常开发默认使用：

GPT-5.6 Sol + High

适用于：

- 页面开发
- Ant Design组件实现
- 表格
- 表单
- Mock数据
- 普通重构
- CSS / UI调整
- TypeScript类型修正
- 常规Bug修复
- API联调
- 已经有明确设计方案的功能实现

---

### 需要升级到高能力模型的任务

以下任务优先建议使用：

GPT-6 Astra + XHigh / 极高

适用于：

- 项目总体架构设计
- 动态表单引擎设计
- 规则引擎设计
- SAP复杂集成设计
- 状态机设计
- 跨模块重构
- 复杂数据一致性问题
- 难以定位的系统性Bug
- 权限体系设计
- 多模块联动问题
- 连续两次使用默认模型仍无法正确解决的问题
- 需要对整个项目进行架构审查时

---

### 使用高能力模型后的处理

高能力模型完成：

- 架构决策
- 复杂问题定位
- 重构方案
- 核心规则设计

之后，应优先回到：

GPT-5.6 Sol + High

继续执行：

- 页面编码
- 普通组件实现
- 样式调整
- 代码补全
- 常规功能迭代

避免长期使用 GPT-6 Astra 处理机械性任务。

---

### Codex 行为要求

当当前任务明显属于高复杂度任务时：

不要直接进行大规模修改。

先输出：

“该任务建议使用 GPT-6 Astra + 极高推理进行设计/分析。”

并说明原因。

当高复杂度问题已经解决、剩余工作属于常规实现时：

应提醒：

“后续可以切回 GPT-5.6 Sol + High 继续开发。”

不得因为模型能力更高，就对简单任务进行过度分析或大规模重构。